spring:
  application:
    name: zmz-client
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: zmz-config
#      uri: http://localhost:8888/